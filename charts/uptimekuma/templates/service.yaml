apiVersion: v1
kind: Service
metadata:
  name: {{ include "uptimekuma.fullname" . }}-server
  labels:
    {{- include "uptimekuma.labelsServer" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5006
      targetPort: 5006
      protocol: TCP
      name: http
  selector:
    {{- include "uptimekuma.selectorLabelsServer" . | nindent 4 }}
---
